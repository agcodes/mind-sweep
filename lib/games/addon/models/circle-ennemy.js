
import CircleHeros from 'games/models/circle-heros';

class CircleEnnemy extends CircleHeros {
  constructor(x, y, width_, height_, fillColor, strokeColor) {
    super(x, y, width_, height_, fillColor, strokeColor);
  }
}

export default CircleEnnemy;