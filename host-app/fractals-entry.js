import Complex from "calc-utils/addon/models/Complex"

// Example usage to ensure they are included in the bundle
const complex = new Complex();

console.log(complex);

// Export the required classes or functions if needed
export { Complex };